namespace TCC.Libs.Hash
{
    public static class Hash
    {
        public static string CreateHash(string email, string senha)
        {
            string hash = " Nosso sistema de hash criado para usar no TCC ";
            return hash;
        }
    }
}
